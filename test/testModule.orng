testModule::Module = (
	someExtern?::Int

	main::(args:[]String)->!() = {
		std.system.getError()
		if someExtern == 4 {
			std.system.println("haha")
		} else {
			std.system.println("hahe")
		}
	}
)