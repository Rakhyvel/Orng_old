testModule::Module = (
	x:ConstInt = 45
	y:ConstInt = 10

	ConstInt::Type = :Int

	A::Type = (|a, b, c:Int)
	B::Type = (|c:Int, d:Char, e)

	FileError::Type = (| 
		fileNotFound
		resourceBusy
	)

	AllocationError::Type = (|
		outOfMemory
		resourceBusy
	)

	givesAnError::(filename:String)->FileError||AllocationError!Int = {
		errdefer std.system.println("Three") 
		{defer std.system.println("Two")}
		errdefer std.system.println("One")
		if filename.length > 4 {
			return FileError.fileNotFound
		} else {
			return 4
		}
	}

	main::(args:[]String)->Int = givesAnError("Hello") catch 0
)