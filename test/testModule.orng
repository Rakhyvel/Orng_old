testModule::Module = (
	globalVar: Int
	globalConst::Int = 5

	f::(x:[5]Char)=>Int = {
		for i := 0; i < x.length; i += 1 {
			x[i] = 'd'
		}
		return globalConst + g(x.length)
	}

	g::(x::Int)=>Int = return x

	+ main::(args:[]String)->Int = {
		return 0
	}
)