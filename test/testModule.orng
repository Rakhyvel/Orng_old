testModule::Module = (
	maybeEven::()->?Int = {
		std.math.seedRand(std.system.clock())
		res: Int = std.math.rand()
		if res % 2 == 0 {return res} else {return nothing}
	}

	main::(args:[]String)->Int = {
		for true {
			std.system.println("working...")
			y: ?Int = maybeEven() orelse break
		}
		0
	}
)