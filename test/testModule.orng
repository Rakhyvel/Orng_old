testModule::Module = (
	A::Type = (|a, b, c:Int)
	B::Type = (|c:Int, d:Char, e)

	FileError::Type = (| 
		fileNotFound
		resourceBusy
	)

	AllocationError::Type = (|
		outOfMemory
		resourceBusy
	)

	givesAnError::(filename:String)->FileError||AllocationError!Int = FileError.fileNotFound

	hmm::()->FileError||AllocationError!Int = {errdefer std.system.println("hmm2"); {errdefer std.system.println("hmm"); try givesAnError("lol")}; std.system.println("done"); 5}

	main::(args:[]String)->Int = hmm() catch 0
)